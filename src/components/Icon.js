import React from 'react';

const button = ({
	icon,
	link
}) => {
	return (
		<a href={link} className="icon" >{children}</a>
	);
};

class Icon extends React.Component {
	render () {
		return (
			<div>
			</div>
		);
	}
}

export default Icon