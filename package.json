{
	"private": true,
	"homepage": ".",
	"dependencies": {
		"-": "^0.0.1",
		"gh-pages": "^3.2.3",
		"next": "^12.1.0",
		"react": "^17.0.2",
		"react-dom": "^17.0.2",
		"smoothscroll-polyfill": "^0.4.4"
	},
	"scripts": {
		"dev": "next dev",
		"build": "next build",
		"start": "next start",
		"export": "next export -o build",
		"predeploy": "npm run build && npm run export && touch ./build/.nojekyll",
		"deploy": "gh-pages -d build -t true"
	}
}
